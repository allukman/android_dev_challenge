<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <gradient
        android:startColor="?attr/colorPrimary"
        android:endColor="@color/black"
        android:angle="45"/> <!-- Sudut gradien (dalam derajat) -->

    <!-- Radius sudut -->
    <corners android:radius="12dp" />
</shape>